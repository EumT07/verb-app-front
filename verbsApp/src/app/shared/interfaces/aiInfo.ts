export interface AiInfo {
    verb: string,
    sentences : {
        present: string[],
        past: string[],
        past_Participle: string[],
        present_Participle: string[],
        future: string[]
    }
}